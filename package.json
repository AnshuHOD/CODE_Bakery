{
  "name": "bakery-automation-root",
  "version": "1.0.0",
  "description": "Root package manager for Render fallback deployment",
  "scripts": {
    "build": "cd backend && npm install",
    "install": "cd backend && npm install",
    "start": "cd backend && node server.js"
  }
}
